package photon.interpreter

import photon.{EValue, Scope}
import photon.core.Core

//class Unbinder(core: Core) {
//  def unbind(value: EValue, toScope: Scope): EValue = {
//    val dependentNames = detectNamesToMove(value.unboundNames, value.scope, toScope)
//  }
//}
